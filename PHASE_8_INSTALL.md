# Phase 8 installation note

This file is installation guidance only. Do not commit it.

Copy the other files to the repository root while preserving paths.

Then run:

```bash
python -m pytest
```

Review the README and `llms.txt`, then commit the Phase 8 files.

Recommended commit:

`Improve repository discovery and AI navigation`
